<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>Usuários</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Ver usuário", user %>
    </p>
  <% end %>
</div>

<%= link_to "Novo usuário", new_user_path %>
